# maasweekly-agent-server.conf：server context 稳定 snippet（Task 06 M8-B3 P0）
#
# 安装位置：/etc/nginx/snippets/maasweekly-agent.conf
# 由 maasweekly-https.conf（被 daily.maas.click 443 server include）引用。
#
# 作用域红线：本文件只允许 server-context 内容（include 指令）。
# 禁止出现 server {、upstream——http 级内容在 conf.d/maasweekly-agent.conf。
# 动态 location（/api/v1/、/api/mcp、feed、Skill）由激活器维护的
# agent-routes.inc 承担；静态 root 由 site-root.inc 承担。

# Agent 路由（REST/MCP/RSS/Skill——继承 site-root.inc 的动态 root）
include /srv/maasweekly/shared/nginx/agent-routes.inc;
