# deploy-mode：部署通道迁移开关（Task 06 M5，D7）
#
# 服务器与三 workflow 共用此文件决定本次发布的部署通道：
#   legacy  aliyun-099 /var/www/maasweekly 的 rsync 直写（当前生产行为，向后兼容）
#   release /srv/maasweekly 的 incoming → 校验 → 蓝绿激活（Task 06 新通道）
#
# 默认 legacy —— 一切现状不变。切换到 release 的时机：
#   授权点 A 通过（Task 06 M7 候选交付被批准）后，
#   在 M8 服务器安装完成且第一次用药 smoke 通过的位置翻转。
#
# 由 ops/deploy-release.sh 与 ops/install-production.sh 读取。
# 仓库内保持 legacy，生产翻转是"安装+翻转"同一个受控操作（手动，非 CI 自动）。
DEPLOY_MODE=legacy
